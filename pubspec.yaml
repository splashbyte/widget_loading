name: widget_loading
description: Simple widget for loading widget contents.
version: 0.1.1
homepage: https://splashbyte.dev

environment:
  sdk: ">=2.10.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: